[project]
name = "mediastack-mcp"
version = "0.1.0"
description = "MCP server for monitoring Synology NAS media stack"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.0.0",
    "httpx>=0.27.0",
]
